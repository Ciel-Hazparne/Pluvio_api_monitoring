{% extends 'base.html.twig' %}

{% block title %}Modification{% endblock %}

{% block body %}
    <h1>Modification des données</h1>
    <link rel="stylesheet" href="{{ asset('css/form.css') }}">
    {{ include('pluviometrie/_form.html.twig', {'button_label': 'Enregistrer'}) }}


<div class="row mt-3">
    <div class="col-md-6 d-flex justify-content-start gap-2">
    <a href="{{ path('app_pluviometrie_index') }}">
        <button type="button" class="btn btn-dark disabled">Retour</button>
    </a>
        {{ include('pluviometrie/_delete_form.html.twig') }}
    </div>
</div>

{% endblock %}
